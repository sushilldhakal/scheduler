---
title: Many blocks demo
description: 1000+ blocks in one view, virtualization demo.
---

# Many blocks demo

1000+ blocks in a single view to demonstrate virtualization and rendering performance.

The docs demo uses generated data from `@/lib/demo/testData` (specifically `perfCategories`, `perfEmployees`, and `perfShifts`) to simulate a large roster.
