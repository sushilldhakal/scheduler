node_modules
*.log
.DS_Store

# Built output — never commit
# All @shadcn-scheduler/* packages build to /dist/<package-name>/ at the repo root
/dist/
# Original monolithic package keeps its own dist inside its folder
packages/shadcn-scheduler/dist/

# Next.js
apps/web/.next/
apps/web/out/
