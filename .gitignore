node_modules
*.log
.DS_Store

# Built output — never commit
# Shared root dist (legacy, superseded by per-package dist/)
/dist/
# Per-package dist directories (built by tsup on CI/Vercel)
packages/*/dist/

# Next.js
apps/web/.next/
apps/web/out/
